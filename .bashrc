[ -n "$PS1" ] && source ~/.bash_profile

#PERL_MB_OPT="--install_base \"/Users/kim/perl5\""; export PERL_MB_OPT;
#PERL_MM_OPT="INSTALL_BASE=/Users/kim/perl5"; export PERL_MM_OPT;

#unset GREP_OPTIONS

#s_hash ()  {
#  echo -n "$1" | sha256sum ;history -d $(history 1)
#}
